apply plugin: "war"

bootWar {
    mainClassName = "ai.giskard.GiskardApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
